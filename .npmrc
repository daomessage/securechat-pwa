# CI / Cloudflare Pages 友好:宽松 peer dep 解析
# 原因:vite-plugin-pwa@1.2.0 peerDeps 只声明到 Vite 7,但项目已用 Vite 8。
#       两者运行时兼容,仅 peerDep 声明滞后;`npm clean-install` 默认严格模式会失败。
legacy-peer-deps=true

# 明确国内/海外镜像可能被 CF 代理触发,这里留官方 registry
registry=https://registry.npmjs.org/
