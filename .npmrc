# CI / Cloudflare Pages 友好配置

# vite-plugin-pwa@1.2.0 peerDep 只声明到 Vite 7,项目已用 Vite 8,兼容但需要宽松解析
legacy-peer-deps=true

# 官方 registry(避免被 CF 构建代理到国内镜像)
registry=https://registry.npmjs.org/

# 强制 git 依赖(如 @daomessage_sdk/sdk 从 GitHub 拉)走 HTTPS 而非 SSH
# CF Pages 构建容器没有 SSH key
git-http-protocol=https
